// ryuu.js — DOMO local development server configuration
// See cli-setup skill for detailed configuration reference

module.exports = {
  // DOMO instance to proxy data requests to
  dev: {
    // Replace with your DOMO instance URL
    proxyTo: 'https://YOUR_INSTANCE.domo.com'
  }
};
